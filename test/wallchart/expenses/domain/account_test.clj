(ns wallchart.expenses.domain.account-test
  (:require [wallchart.expenses.domain.account :as sut]
            [midje.sweet :refer :all]))

(facts "when creating a new expense account"
  (fact "it returns new account data"))
